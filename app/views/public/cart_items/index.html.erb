
<table>
 <tr>
  <td>商品名</td>
  <th>単価(税込)</th>
  <th>数量</th>
  <th>小計</th>
  <td></td>
 </tr>
 <% @cart_items.each do |cart_item| %>
 <tr>
  <td><%= cart_item.item.name %></td>
  <td><%= cart_item.item.with_tax_price %></td>
  <td><%= cart_item.amount %></td>
  <td><%= cart_item.subtotal %></td>
  <td><%= link_to "消去する", cart_item_path(cart_item.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
 </tr>
 <% end %>
</table>

<%= @total %>
<%= link_to "全て消去する", destroy_all_path, method: :delete, "data-confirm" => "本当に削除しますか？" %>
<%= link_to '情報入力画面へ進む', new_order_path %>