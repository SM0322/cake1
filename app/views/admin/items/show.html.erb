<div class='container'>
  <div class='row'>
    <h2>商品詳細</h2>
  </div>
</div>

<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto pt-5">
      <p><%= "商品名" %>　<%= @item.id %></p>
      <p><%= "商品詳細" %>　<%= @item.introduction %></p>
      <p><%= "ジャンル" %>　<%= @item.genre.name %></p>
      <p><%= "税込価格(税抜価格)" %>　<%= @item.price %></p>
      <p>
        <%= "販売ステータス" %>　
        <% if @item.is_active == true %>
          <p class="text-success">販売中</p>
        <% else %>
          <p class="text-muted">販売中止中</p></td>
        <% end %>
      </p>
      <p><%= link_to "編集する", edit_admin_item_path(@item.id) %></p>
    </div>
  </div>
</div>

